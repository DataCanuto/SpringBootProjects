# aula-spring-data-jpa

Introdução ao Spring Data JPA: criação de uma entidade `User` e do respectivo repositório JPA para operações de persistência.

## Conteúdo

- `model/User.java`
- `repository/UserRepository.java`
- `StartApp.java`

## Como rodar

```bash
./mvnw spring-boot:run
```
